// STRING TYPE -

// Declaring and Initializing String
let f_Name: string = "Mark";

// String Expression
let fullName: string = f_Name + " " + "Mark";

// String Methods
let nameComp: string = fullName.toUpperCase();
