// MATHS INTRODUCTION :

// The JavaScript Math object allows us to perform mathematical operations on numbers very easily.

// let num1 = 25.25;
// let num2 = -80;

// console.log(Math.PI);

// console.log(Math.round(num1));
// console.log(Math.abs(num2));

// console.log(Math.sqrt(num1));
// console.log(Math.pow(2,3)); 
// // can also be done like -> 2**3

// console.log(Math.ceil(num1));
// console.log(Math.floor(num1));

// console.log(Math.min(num1, num2));
// console.log(Math.max(num1, num2));

// console.log(Math.random(num1, num2));
// creates a random number between 0(inclusive) and 1(exclusive)
// console.log(Math.floor(Math.random() * 10)); // 0-9 output


// console.log(Math.trunc(4.6));
// console.log(Math.trunc(-99.23));
// It returns only the integer part of a number.
// Note : If the argument is positive, Math.trunc() result is equivalent to Math.ceil(), otherwise Math.trunc() result is equivalent to Math.floor().